[build]
builder = "nixpacks"

[deploy]
startCommand = "playwright install chromium && playwright install-deps chromium && gunicorn server:app --workers 1 --timeout 60 --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
